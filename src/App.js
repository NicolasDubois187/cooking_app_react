import React from "react";

const App = () => {
  return (
    <div>
      <h1>Cooking-app</h1>
      {/* //lien sur : themealDB (tester le lien) 
      //créer variable pour stocker les recettes (useState)
      //Aller chercher les données avec axios via le
      useEffect puis les stocker.
      //Afficher les données à l'écran (Nom, origine, photo, lien de la video)
      // Rechercher un repas via un input, filter
      // BONUS
        // proposer des boutons avec spécialités de certains pays (french, itailan, spanish)
       */}
    </div>
  );
};

export default App;
